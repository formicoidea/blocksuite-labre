---
'@labre/std': patch
'@labre/affine-widget-edgeless-toolbar': patch
'@labre/affine': patch
---

The catalogue leads with what you reach for

The sidepanel now opens on a "Recent & frequent" head section (PO recette,
27/08/2026): the same 4-most-used + 3-most-recent arbitration the senior
sub-menu runs, re-consumed whole through the new pure
`rankCommandsByUsage` — one ranking, two consumers, never two opinions.
Only commands that actually carry a measure appear, so a fresh install shows
no section rather than a "recent" label padded with the never-used; the rows
repeat below in their categories, the way every launcher does it.
