---
'@labre/affine-gfx-mindmap': patch
---

Render the mindmap toolbar icon at its natural size (was capped smaller than the
neighbouring text / add-file / shape icons), so the senior tool row reads
homogeneously.
