---
'@labre/affine-block-root': patch
'@labre/affine-block-surface': patch
'@labre/affine-shared': patch
'@labre/std': patch
---

The bundled HTML/SVG sanitizer moves to DOMPurify 3.4.13 or later, so a pasted or imported drawing is cleaned by a version with no known sanitization bypass.
