// The canvas (2d context) renderers still live in `../element-renderer.ts`
// (brush) and in `@labre/affine-gfx-shape` (highlighter); only the DOM
// variants are collected here.
export * from './dom/index.js';
