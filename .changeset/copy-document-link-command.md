---
'@labre/affine-block-root': minor
'@labre/affine-shared': patch
---

Copy a link to the current document with Ctrl/Cmd+Alt+L, anchored on the selected blocks or canvas elements, ready to be pasted into a text as a reference — available wherever the host registers `GenerateDocUrlExtension`.
